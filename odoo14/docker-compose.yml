version: '3.7'
services:
  web:
    image: odoo:14.0
    container_name: odoo14c
    networks:
      - default
      - pg_default
    external_links:
      - db:postgres
    ports:
      - "8170:8069"
    volumes:
      - odoo-web-data:/var/lib/odoo      
      - ./etc:/etc/odoo
      - ./extra-addons:/mnt/extra-addons
networks:
      default:
          driver: bridge
      pg_default:
          external: true      
volumes:
  odoo-web-data:        

